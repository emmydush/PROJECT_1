# Django Settings
DEBUG=False
SECRET_KEY=your-secret-key-here-generate-a-random-string
# IMPORTANT: Update ALLOWED_HOSTS to match your Render domain (e.g., project-xh5v.onrender.com)
ALLOWED_HOSTS=project-xh5v.onrender.com,www.project-xh5v.onrender.com,localhost,127.0.0.1

# Database
# Set a full DATABASE_URL for PostgreSQL. Example formats we accept:
# - Render / managed Postgres:
#   postgresql://username:password@db.abc123.render.com:5432/dbname
# - Local / Docker Compose (service named `db`):
#   postgresql://postgres:password@db:5432/inventory_db
# IMPORTANT: Do NOT leave DATABASE_URL empty — the app will try to parse it.
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
EMAIL_USE_TLS=True

# Security Headers (set to True in production with HTTPS)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# CORS Settings (if needed for API)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Django Admin Email
ADMIN_EMAIL=admin@yourdomain.com

# Notes:
# - Render provides a DATABASE_URL after creating a PostgreSQL managed service;
#   copy that exact string into Render Web Service environment variables.
# - If you are deploying with Docker Compose and your Postgres service is
#   named `db`, use the hostname `db` in the connection string (postgresql://...@db:5432/...).
# - If you want the app to fallback to a local SQLite DB when DATABASE_URL
#   is not set, leave DATABASE_URL unset locally and ensure local_settings.py
#   is present for development overrides.

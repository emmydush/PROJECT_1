# Generated by Django 5.2.6 on 2025-11-07 20:55

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('purchases', '0006_purchaseorder_branch'),
        ('superadmin', '0004_branch_manager'),
    ]

    operations = [
        # Check if the column exists before adding it
        migrations.RunSQL(
            # Forward operation - only add column if it doesn't exist
            """
            DO $$
            BEGIN
                IF NOT EXISTS (
                    SELECT 1 FROM information_schema.columns 
                    WHERE table_name='purchases_purchaseorder' AND column_name='branch_id'
                ) THEN
                    ALTER TABLE purchases_purchaseorder 
                    ADD COLUMN branch_id integer REFERENCES superadmin_branch(id) DEFAULT NULL;
                END IF;
            END
            $$;
            """,
            # Reverse operation - remove column if it exists
            """
            DO $$
            BEGIN
                IF EXISTS (
                    SELECT 1 FROM information_schema.columns 
                    WHERE table_name='purchases_purchaseorder' AND column_name='branch_id'
                ) THEN
                    ALTER TABLE purchases_purchaseorder DROP COLUMN branch_id;
                END IF;
            END
            $$;
            """
        ),
    ]